---
phase: 7
plan: 1
wave: 1
depends_on: []
files_modified:
  - src/data/pc-components.json
autonomous: true
must_haves:
  truths:
    - "Each component has detailed spec fields for compatibility checking"
    - "CPUs have socket, ddrGeneration, tdp fields"
    - "GPUs have tdp, recommendedPsu fields"
    - "RAM has ddrGeneration, speed, capacity fields"
    - "PSUs have wattage, rating fields"
    - "Cases have formFactor, maxGpuLength fields"
    - "All purchase link URLs point to real product pages where possible"
  artifacts:
    - "src/data/pc-components.json has enriched spec data"
---

# Plan 7.1: Expand Component Data Schema

<objective>
Enrich pc-components.json with detailed technical specifications for each component. This data powers the enhanced compatibility checker in Plan 7.2.

Purpose: Replace string-matching heuristics with proper spec-based compatibility validation.
Output: An expanded JSON database with spec fields on every component.
</objective>

<context>
Load for context:
- src/data/pc-components.json (current schema)
</context>

<tasks>

<task type="auto">
  <name>Add Spec Fields to All Components</name>
  <files>src/data/pc-components.json</files>
  <action>
    Expand every component entry with technical spec fields. Keep existing fields (name, price, purchaseLinks) and ADD new ones:

    **CPU specs to add:**
    ```json
    "socket": "LGA1700" | "AM5",
    "ddrGeneration": "DDR5" | "DDR4/DDR5",
    "tdp": 125,
    "cores": 14,
    "brand": "Intel" | "AMD"
    ```

    Accurate values:
    - i5-13600K: LGA1700, DDR4/DDR5, 125W TDP, 14 cores
    - i7-13700K: LGA1700, DDR4/DDR5, 125W TDP, 16 cores
    - Ryzen 5 7600X: AM5, DDR5, 105W TDP, 6 cores
    - Ryzen 7 7800X3D: AM5, DDR5, 120W TDP, 8 cores
    - i9-13900K: LGA1700, DDR4/DDR5, 125W TDP, 24 cores

    **GPU specs to add:**
    ```json
    "tdp": 160,
    "recommendedPsu": 550,
    "length": 240,
    "brand": "NVIDIA" | "AMD"
    ```

    Accurate values:
    - RTX 4060 Ti: 160W TDP, 550W PSU, 240mm
    - RTX 4070: 200W TDP, 650W PSU, 244mm
    - RTX 4080: 320W TDP, 750W PSU, 304mm
    - RX 7700 XT: 245W TDP, 700W PSU, 267mm
    - RX 7900 XT: 315W TDP, 750W PSU, 276mm

    **RAM specs to add:**
    ```json
    "ddrGeneration": "DDR5" | "DDR4",
    "speed": 5600,
    "capacity": 16,
    "modules": 2
    ```

    **Storage specs to add:**
    ```json
    "type": "NVMe SSD" | "HDD",
    "capacity": "1TB" | "2TB",
    "interface": "PCIe 4.0" | "SATA"
    ```

    **PSU specs to add:**
    ```json
    "wattage": 750,
    "rating": "80+ Gold" | "80+ Bronze",
    "modular": true | false
    ```

    **Case specs to add:**
    ```json
    "formFactor": "ATX" | "Micro-ATX",
    "maxGpuLength": 360,
    "fans": 3
    ```

    Also update prices to more accurate INR values where needed.

    AVOID: Changing the existing purchaseLinks structure. Only ADD new fields.
    AVOID: Removing any existing components.
  </action>
  <verify>node -e "const d=require('./src/data/pc-components.json'); console.log(d.categories.CPU[0].socket, d.categories.GPU[0].tdp, d.categories.RAM[0].ddrGeneration)"</verify>
  <done>Every component entry in pc-components.json has spec fields (socket/ddr/tdp/wattage/formFactor etc.).</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] CPU entries have socket, ddrGeneration, tdp, cores, brand
- [ ] GPU entries have tdp, recommendedPsu, length, brand
- [ ] RAM entries have ddrGeneration, speed, capacity
- [ ] PSU entries have wattage, rating
- [ ] Case entries have formFactor, maxGpuLength
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
