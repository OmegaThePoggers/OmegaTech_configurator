---
phase: 2
plan: 2
wave: 1
---

# Plan 2.2: Setup Configurations Page & Routing

## Objective
Establish the route for the "Saved Builds" functionality (`/configs`) and ensure all navigation links across the app work correctly.

## Context
- .gsd/SPEC.md
- legacy/configurations.html
- src/components/layout/navbar.tsx
- src/components/layout/footer.tsx

## Tasks

<task type="auto">
  <name>Scaffold Configs Page</name>
  <files>src/app/configs/page.tsx, legacy/configurations.html</files>
  <action>
    - Create `src/app/configs/page.tsx`.
    - Implement a placeholder grid layout for saved builds representing the structure found in `legacy/configurations.html`.
    - Use a simple "No builds saved yet" empty state for now.
    - Apply Tailwind classes that match the dark theme.
  </action>
  <verify>npm run build</verify>
  <done>The /configs route is accessible and displays an empty state grid.</done>
</task>

<task type="auto">
  <name>Verify Navigation Routing</name>
  <files>src/components/layout/navbar.tsx, src/components/layout/footer.tsx</files>
  <action>
    - Update Next.js `<Link>` references in the Navbar and Footer to ensure they point accurately to `/`, `/about`, `/contact`, and `/configs`.
    - Add active state styling indications if easy, or just ensure paths are correct.
  </action>
  <verify>grep -r 'href="' src/components/layout</verify>
  <done>All navigation links point correctly to Next.js routes, not .html files.</done>
</task>

## Success Criteria
- [ ] The `/configs` route exists and loads without error.
- [ ] The Navbar connects seamlessly to Home, Build PC (placeholder), Saved Configs, and Contact.
- [ ] The Footer connects seamlessly to About and Contact.
