---
phase: 3
plan: 2
wave: 2
---

# Plan 3.2: Build Configurator UI Shell

## Objective
Create the React component layout for `/build` (the Configurator page). Establish the left and right panels matching the legacy layout, utilizing shadcn UI and Tailwind.

## Context
- .gsd/SPEC.md
- legacy/configurator.php
- src/app/build/page.tsx

## Tasks

<task type="auto">
  <name>Scaffold Configurator Page Structure</name>
  <files>src/app/build/page.tsx</files>
  <action>
    - Create `src/app/build/page.tsx`.
    - Build a two-column responsive layout (Left: Component Selection & 3D Viewer space. Right: Summary & Stats).
    - Use `Card` components for the summary sidebar.
    - Add a placeholder `div` where the 3D Viewer will go later (matching `#3d-container` from legacy).
    - Hardcode the categories (CPU, GPU, RAM, etc.) to visually match the legacy Select dropdowns using standard HTML `<select>` styled with Tailwind, or wait for state integration. For this task, just create the static visual shells.
  </action>
  <verify>npm run build</verify>
  <done>The /build route renders a two-column layout visually resembling the configurator wireframe.</done>
</task>

## Success Criteria
- [ ] `src/app/build/page.tsx` exists and loads without error.
- [ ] Layout consists of a main component area and a sticky summary sidebar.
